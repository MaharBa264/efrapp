import test from 'node:test';import assert from 'node:assert/strict';
import {reportRange} from '../report-range.js';
import {receiptSvg} from '../web/receipt.js';
test('date range includes both local calendar days with exclusive UTC end',()=>{assert.deepEqual(reportRange('2026-09-01','2026-09-30'),{start:'2026-09-01T03:00:00.000Z',end:'2026-10-01T03:00:00.000Z'});assert.equal(reportRange('2026-02-30','2026-03-01'),null);assert.equal(reportRange('2026-10-01','2026-09-30'),null)});
test('receipt includes only available customer contact and keeps item rows below header',()=>{let d={number:7,created_at:'2026-09-24T15:00:00Z',customer_name:'Cliente',seller_name:'Vendedor',issuer_snapshot:'{}',items:[{product_name:'LAMINADO VERDE',units_count:5,weight_kg:2.5}]};let both=receiptSvg({...d,customer_direction:'Calle 123',customer_phone:'266-123'});assert.match(both.svg,/Domicilio: Calle 123/);assert.match(both.svg,/Teléfono: 266-123/);assert.match(both.svg,/LAMINADO VERDE/);assert.equal(both.height,900);let one=receiptSvg({...d,customer_phone:'266-123'}).svg;assert.doesNotMatch(one,/Domicilio:/);assert.match(one,/Teléfono: 266-123/);assert.doesNotMatch(receiptSvg(d).svg,/Domicilio:|Teléfono:/)});
